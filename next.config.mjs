/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com', 'localhost', 'res.cloudinary.com'], // Add all domains you need
      },
};

export default nextConfig;
